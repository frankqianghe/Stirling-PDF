/**
 * Core stub – renders nothing.
 * The desktop build overrides this to absolute-position the paywall entry pill
 * at the top-right corner of the Workbench (the middle PDF content area).
 */
export function WorkbenchCornerExtensions() {
  return null;
}
